#!/bin/bash

while read file
do
    case "$1" in
        "w")
            prompt "Set $file as Wallpaper?" && feh --bg-fill "$file" & ;;
        "x")
            prompt "Set $file as LockScreen?" && betterlockscreen -u "$file" & ;;
        "d")
		    prompt "Delete $file?" && rm "$file" ;;
        "D")
            rm "$file" ;;
   esac
done

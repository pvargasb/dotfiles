ICON="%{$fg_bold[white]%}"
if [ $DISPLAY ]; then
    ICON+="λ"
else
    ICON+="->"
fi

DIR="%{$fg_bold[blue]%}%1~/"
SUFFIX="%{$reset_color%}"

PROMPT="${ICON} ${DIR} ${SUFFIX}"
RPROMPT="%(?,,%{$fg[red]%}●) ${SUFFIX}"
